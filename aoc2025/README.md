# Advent of Code 2025

1. Put files within `inputs/` in the format `dayDD.txt`, where `DD` is 0-padded
2. To run, do 
  `mix aoc.run DAY [PART] [--file <input_path>]`
  or in IEX:
  `iex> AOC2025.Runner.run_file(1, 1)`
3. Doctests can be run using `mix test`



